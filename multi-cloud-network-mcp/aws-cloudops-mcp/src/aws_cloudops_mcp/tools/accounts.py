"""Reserved for future multi-account discovery tools.

Milestone 1 ships a single-account/single-role identity tool
(``aws_get_caller_identity`` in :mod:`aws_cloudops_mcp.tools.identity`).
This module is a placeholder for a future ``aws_list_accounts`` /
AWS Organizations-aware discovery tool, kept as its own module now so that
addition does not require restructuring the ``tools`` package later.
"""

from __future__ import annotations
